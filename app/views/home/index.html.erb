<%= link_to('app_setting for super admin', super_admins_app_setting_path) if is_super_admin? %>
<%= link_to('app_setting for app admin', super_admins_app_setting_path) if is_app_admin? %>

<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<%= link_to 'logout', destroy_user_session_path, method: :delete %>